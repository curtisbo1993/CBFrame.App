<Window x:Class="CBFrame.App.Wpf.Views.Dialogs.SectionSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Section"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        MinWidth="700"
        MinHeight="400"
        ResizeMode="CanResizeWithGrip">

    <DockPanel Margin="12">

        <!-- Filter row -->
        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    Margin="0,0,0,8">
            <TextBlock Text="Filter:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0" />
            <TextBox Width="200"
                     VerticalAlignment="Center"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!-- Grid of sections -->
        <DataGrid ItemsSource="{Binding SectionsView}"
                  SelectedItem="{Binding SelectedSection, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="*" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Type" Binding="{Binding ShapeType}" Width="*" />
                <DataGridTextColumn Header="Area" Binding="{Binding Area}" Width="*" />
                <DataGridTextColumn Header="Ix" Binding="{Binding Ix}" Width="*" />
                <DataGridTextColumn Header="Iy" Binding="{Binding Iy}" Width="*" />
                <DataGridTextColumn Header="w/ft" Binding="{Binding WeightPerLength}" Width="*" />
                <DataGridTextColumn Header="Material" Binding="{Binding MaterialId}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="80"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Command="{Binding OkCommand}" />
            <Button Content="Cancel"
                    Width="80"
                    IsCancel="True"
                    Command="{Binding CancelCommand}" />
        </StackPanel>

    </DockPanel>
</Window>
