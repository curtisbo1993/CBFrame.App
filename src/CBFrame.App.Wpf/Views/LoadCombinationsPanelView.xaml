<UserControl x:Class="CBFrame.App.Wpf.Views.LoadCombinationsPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DockPanel Margin="12">
        <!-- Buttons row -->
        <StackPanel Orientation="Horizontal"
                    DockPanel.Dock="Top"
                    Margin="0 0 0 10">
            <Button Content="Add"
                    Width="60"
                    Margin="0 0 8 0"
                    Command="{Binding AddComboCommand}" />
            <Button Content="Delete"
                    Width="60"
                    Margin="0 0 8 0"
                    Command="{Binding DeleteComboCommand}" />
            <Button Content="Defaults"
                    Width="70"
                    Command="{Binding GenerateDefaultsCommand}" />
        </StackPanel>

        <!-- Grid of combinations -->
        <DataGrid ItemsSource="{Binding Combinations}"
                  SelectedItem="{Binding Selected}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding Id}"
                                    Width="40"
                                    IsReadOnly="True" />
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    Width="*" />
                <DataGridComboBoxColumn Header="Type"
                                        SelectedItemBinding="{Binding Type}"
                                        ItemsSource="{Binding CombinationTypes,
                                                             RelativeSource={RelativeSource AncestorType=UserControl}}" />
                <DataGridCheckBoxColumn Header="Active"
                                        Binding="{Binding IsActive}"
                                        Width="60" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
